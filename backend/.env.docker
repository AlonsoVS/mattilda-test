# Docker Development Environment
# Use this for running the application with docker-compose

# Database Configuration (Docker service names)
DATABASE_URL=postgresql://mattilda_user:mattilda_password@db:5432/mattilda_db
DATABASE_HOST=db
DATABASE_PORT=5432
DATABASE_NAME=mattilda_db
DATABASE_USER=mattilda_user
DATABASE_PASSWORD=mattilda_password

# Application Configuration
ENVIRONMENT=development
DEBUG=true
LOG_LEVEL=info

# Database Settings
DB_ECHO=true

# API Settings
API_V1_STR=/api/v1
PROJECT_NAME=Mattilda API
PROJECT_DESCRIPTION=School Management System API
VERSION=1.0.0

# Security (development only - not secure)
SECRET_KEY=H51GZkhlCS5sieAzodPcCGl_EOcMTJmw8W9XHcovZEo
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Default Admin User (for Docker environment)
ADMIN_USERNAME=admin
ADMIN_EMAIL=admin@mattilda.com
ADMIN_PASSWORD=admin123
ADMIN_FULL_NAME=Admin User

# CORS Settings (permissive for development)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080,http://localhost:3001,http://127.0.0.1:3000,http://localhost:5173,http://127.0.0.1:5173

# Application Server
APP_HOST=0.0.0.0
APP_PORT=8000
RELOAD=true

# pgAdmin Configuration (for docker-compose tools profile)
PGADMIN_DEFAULT_EMAIL=admin@mattilda.com
PGADMIN_DEFAULT_PASSWORD=admin123
